# grid
cell = triangle
element = FiniteElement("P", cell, 1)

v = TestFunction(element)

alpha = Constant(cell)
gamma = Constant(cell)
dt = Constant(cell)

unew = Coefficient(element)
uold = Coefficient(element)
L = alpha*inner((unew - uold)/dt, v)*dx + gamma*inner(grad(unew), grad(v))*dx
